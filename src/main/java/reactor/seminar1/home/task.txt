1. Доработать метод getWeatherForecastInSofia.
    - Изменить сигнатуру getWeatherForecastByLatitudeAndLongitude и добавить входные параметры (latitude и longitude)
      На сайте https://open-meteo.com/en/docs ознакомиться, какие города поддерживает апи.
    - Для subscribe() метода - в аргументы передать реализованный метод onNext, который будет приводить запрос к виду
        {
            "2022-10-26T00:00", 19.5
            "2022-10-26T01:00", 18.3
            ......
        }
      и выводить его на экран

2. Реализовать метод Mono<Boolean> applyForVisa(int age, int averageIncome, String countryName) сервиса VisaService
   - Если документы на визу подаются в Финляднию (Finland) - вернуть ошибку с текстом "Пока не принимаем"
   - Документы на визу примут у всех, кому больше 18 лет (age) и средний доход (averageIncome) больше 30_000 руб. Результат true (принята)
   - Для всех остальных должен быть получен результат false (отклонена)

3. Создать FileService
    Сервис должен уметь:
        - читать файл и возвращать содержимое
        - создавать файл и записывать контент
        - удалять файл

    Файлы должны храниться в src/main/resources/assignment/1
    Вся логика должна выполняться ТОЛЬКО ТОГДА, КОГДА подписывается клиент
    В случае ошибок возвращать Mono.error(() -> new YOUR_EXCEPTION_WITH_MESSAGE)
    Необязательно: Используйте класс Files и его апи